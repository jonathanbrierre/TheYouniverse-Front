const initialState = {
    user: {
        thing: 'hello'
    }
}
export default function userManager(state = initialState, action){
    switch(action.type){
        default:
            return state
    }
}